---
title: "Guía para principiantes: Uso de la línea de comandos de Linux para la ciberseguridad"
date: 2023-03-13
toc: true
draft: false
descripción: "Aprende a usar la línea de comandos de Linux para ciberseguridad con comandos básicos y avanzados".
tags: ["Linux", "Línea de comandos", "Ciberseguridad", "Guía para principiantes", "Escaneo de red", "Pruebas de vulnerabilidad", "Análisis de malware", "Permisos", "Tráfico de red", "Estado de procesos", "Estadísticas de red", "Búsqueda de archivos", "Wireshark", "TCPDump", "Nmap", "Linux CLI", "Seguridad", "Pruebas de penetración", "Forense digital"].
cover: "/img/cover/A_cartoon_illustration_of_a_person_wearing_a_hoodie.png"
coverAlt: "Ilustración de dibujos animados de una persona que lleva una sudadera con capucha, sentada frente a la pantalla de un ordenador con la interfaz de línea de comandos de Linux visible y sosteniendo una lupa para representar el aspecto de la ciberseguridad."
coverCaption: ""
---
```bash
$ pwd
/home/usuario
$ cd documentos
$ pwd
/home/usuario/documentos
```
```bash
$ ls documentos
archivo1.txt archivo2.pdf archivo3.docx
```
Bash
$ ls
Escritorio Documentos Descargas Música Imágenes Público Plantillas Vídeos
```
``bash
$ cd documentos
```
Bash
$ cd /usr/local/bin
```
Bash
$ cd ../..
```
```bash
$ cat archivo1.txt
```
```bash
$ cat fichero1.txt fichero2.txt fichero3.txt
```
``bash
$ cat *.txt
```
```bash
$ grep "error" logfile.txt
```
```bash
$ grep "p.*y" logfile.txt
```
```
4 = leer
2 = escribir
1 = ejecutar
```
```bash
$ chmod 644 archivo1.txt
```
```bash
$ chmod ug=rw,o= archivo2.txt
```
```bash
$ nmap 192.168.1.1
```
```bash
$ nmap 192.168.1.1-255
```
```bash
$ sudo tcpdump -i eth0
```
```bash
$ sudo tcpdump host 192.168.1.1
```
```bash
$ sudo tcpdump -i eth0 -w capture.pcap
```
```bash
$ ps aux
```
```bash
$ ps -p 1234
```
```bash
$ ps -p 1234 -o %cpu,%mem
```
``bash
$ netstat -a
```
```bash
$ netstat -at
```
```bash
$ netstat -st
```
```bash
$ find . -nombre ejemplo.txt
```
```bash
$ find . -tamaño +1M
```
``bash
$ find . -mtime -7
```
  **Linux es un sistema operativo polivalente y seguro muy utilizado en el sector de la ciberseguridad debido a su naturaleza de código abierto. Sin embargo, puede resultar descorazonador para los principiantes utilizar la interfaz de línea de comandos Linux (CLI) para realizar tareas de ciberseguridad. Esta guía pretende proporcionar a los principiantes una visión general de los comandos CLI Linux básicos y avanzados útiles para la ciberseguridad. ## Commandes Linux de base pour la cybersécurité ### Imprimer le répertoire de travail La commande **pwd** (print working directory) est utilisée pour afficher le répertoire de travail actuel dans la CLI. El directorio de trabajo es el directorio en el que se encuentra actualmente en el sistema de archivos. El comando es útil para navegar por el sistema de archivos y comprender su ubicación en relación con otras carpetas. Por ejemplo, si te encuentras en el directorio de personal y quieres acceder al directorio de documentos, puedes utilizar los siguientes comandos : En el ejemplo siguiente, el primer comando imprime el directorio de trabajo actual, que es el directorio de personal. El segundo comando cambia el directorio a directorio de documentos y el tercero imprime el directorio de trabajo actual, que es actualmente el directorio de documentos. ### Liste El comando **ls** se utiliza para ver el contenido de una carpeta en la CLI. El comando muestra los nombres de los archivos y carpetas en la lista de trabajo actual. El comando es útil para identificar los archivos y las carpetas en un lugar específico. Por ejemplo, si desea ver el contenido del directorio de documentos, puede utilizar el comando siguiente : En el ejemplo siguiente, el comando muestra el contenido del directorio de documentos, que contiene tres archivos: archivo1.txt, archivo2.pdf y archivo3.docx. También puede utilizar el comando sin ningún argumento para revisar el contenido actual del directorio de trabajo. Por ejemplo: En el ejemplo siguiente, el comando muestra el contenido de la base de datos, que contiene varios sub-pertorios, como Oficina, Documentos, Descargas, etc. ### Changeur de répertoire La commande **cd** (change directory) est utilisée pour changer le répertoire de travail actuel dans la CLI. El comando permite navegar por el sistema de archivos y acceder a archivos de diferentes ubicaciones. Por ejemplo, si desea reemplazar el directorio de trabajo actual por el directorio de documentos, puede utilizar el siguiente comando : En el ejemplo siguiente, el comando sustituye la carpeta de trabajo actual por la carpeta de documentos, que se encuentra en la carpeta de trabajo actual. También puede utilizar el comando con una ruta absoluta o relativa para reemplazar la carpeta de trabajo por una carpeta que no se encuentre en la carpeta de trabajo actual. Por ejemplo: En el ejemplo anterior, el comando reemplaza la carpeta de trabajo actual por la carpeta /usr/local/bin, que es una ruta absoluta. También puede utilizar una ruta relativa para modificar el directorio de trabajo. Por ejemplo: En el ejemplo siguiente, el comando modifica la lista de tareas actual de dos niveles por debajo de la lista de tareas actual. La notación ".." representa el directorio principal y puede utilizarla para navegar por el árbol de directorios. ### Concatenar El comando **cat** (concatenar) se utiliza para adjuntar el contenido de un archivo en la CLI. Este comando es útil para adjuntar el contenido de archivos de texto, como archivos de diario o archivos de configuración. Por ejemplo, si a continuación desea mostrar el contenido de un archivo llamado "fichier1.txt", puede utilizar el siguiente comando : En el ejemplo siguiente, el comando muestra el contenido del archivo "archivo1.txt". También puede utilizar el comando para concatenar varios archivos y mostrar su contenido en la CLI. Por ejemplo: en el ejemplo siguiente, el comando muestra el contenido de tres archivos: archivo1.txt, archivo2.txt y archivo3.txt. También puede utilizar el comando con caracteres genéricos para concatenar todos los archivos correspondientes a un modelo específico. Por ejemplo: en el ejemplo siguiente, el comando muestra el contenido de todos los archivos que tienen la extensión ".txt" en la carpeta de trabajo actual. Este comando es útil para mostrar rápidamente el contenido de varios archivos sin tener que abrirlos individualmente. ### Impression d'expressions régulières global El comando **grep** (impression d'expression régulière globale) se utiliza para buscar cadenas o modelos específicos en un archivo o conjunto de archivos en la CLI. El comando es útil para identificar modelos en los archivos de diario o para buscar información específica en los archivos de configuración. Por ejemplo, si desea buscar todas las apariciones del término "error" en un archivo llamado "logfile.txt", puede utilizar el comando siguiente : En el ejemplo siguiente, el comando busca todas las apariciones de la palabra "error" " " " " " en el archivo "logfile.txt" y muestra las líneas correspondientes en la CLI. También puede utilizar el comando con expresiones especiales para buscar modelos más complejos. Por ejemplo, si desea buscar todas las líneas que contengan una palabra compuesta por "p" y terminada en "y", puede utilizar el comando siguiente : En el ejemplo siguiente, el comando busca todas las líneas que contengan una palabra que empiece por "p" y termine por "y" en el archivo "logfile.txt". La expresión regular "p.*y" corresponde a cualquier cadena que empiece por "p" y termine por "y", con cualquier número de caracteres entre ellos. Este comando es útil para buscar modelos en los archivos de diario que puedan ayudar a identificar amenazas de seguridad o problemas de rendimiento. ### Changer de mode El comando **chmod** (modificar el modo) se utiliza para modificar las autorizaciones de un archivo o de un repertorio en la CLI. Este comando es esencial para proteger los archivos y carpetas y controlar el acceso a los mismos. Las autorizaciones están representadas por tres marcadores que corresponden respectivamente al propietario, al grupo y a otros. Las cifras se calculan de la siguiente manera: Por ejemplo, si desea conceder al propietario autorizaciones de lectura y escritura y al grupo y a los demás autorizaciones de lectura únicamente sobre un fichero llamado "fichero1.txt", puede utilizar el comando siguiente : En el ejemplo siguiente, el comando define las autorizaciones del archivo "archivo1.txt" en 644. La primera cifra (6) representa las autorizaciones para el propietario, que son en lectura y en escritura (4 + 2 = 6) . La segunda cifra (4) representa las autorizaciones para el grupo, que está sólo en lectura. La tercera cifra (4) representa las autorizaciones para los demás, que también están en lectura única. También puede utilizar el comando con letras para modificar las autorizaciones. Por ejemplo, si desea conceder al propietario y al grupo autorizaciones de lectura y escritura y a los demás ninguna autorización sobre un fichero llamado "fichero2.txt", puede utilizar el comando siguiente : En el ejemplo siguiente, el comando define las autorizaciones del fichero "file2.txt" en ug=rw,o=, donde "ug" representa al propietario y al grupo, "r" representa la autorización de lectura y "w" representa la autorización de escritura. . El signo "=" significa "definir las autorizaciones para" y "o=" significa "suprimir todas las autorizaciones para los demás". Este comando es útil para controlar el acceso a archivos y carpetas sensibles y evitar el acceso no autorizado. ## Commandes Linux avancées pour la cybersécurité ### Mappeur de réseau El comando **nmap** es una poderosa herramienta de análisis de red en la CLI que puede ser utilizada para identificar los hosts y los servicios en una red, así como las vulnerabilidades potenciales. El comando puede llevar a cabo una serie de técnicas de análisis, entre las que se incluyen la detección de huéspedes, el análisis de puertos y la detección del sistema de explotación. Uno de los usos más elementales de nmap consiste en analizar una única dirección IP o un único host. Por ejemplo, para analizar una dirección IP única (192.168.1.1) para los puertos abiertos, puede usar el siguiente comando : Este comando realiza un análisis TCP SYN sobre la dirección IP indicada y muestra una lista de los puertos abiertos. La salida muestra los puertos abiertos así como el servicio en ejecución en cada puerto, el estado del puerto (abierto/cerrado) y a veces información adicional como el sistema de explotación en ejecución en la dirección. Nmap también puede utilizarse para analizar varias direcciones IP a la vez. Por ejemplo, para analizar un grupo de direcciones IP (192.168.1.1-255) en busca de puertos abiertos, puede utilizar el siguiente comando : Este comando analiza todas las direcciones IP de la red y muestra los puertos y servicios abiertos para cada dirección. Además de la detección de host de base y el análisis de puertos, nmap también puede realizar análisis más avanzados como la detección de servicios y versiones, la detección de sistemas de explotación y el análisis de vulnerabilidad. Estos análisis pueden ser útiles para identificar las vulnerabilidades de seguridad potenciales en una red y la protección contra ataques potenciales. ### Videur de paquets TCP El comando **tcpdump** se utiliza para capturar y analizar el tráfico de red en la CLI, lo que lo hace útil para resolver los problemas de red, analizar el comportamiento de la red e identificar las amenazas de seguridad potenciales. El comando captura los paquetes en tiempo real y puede filtrarlos en función de varios criterios, como la dirección IP de origen o de destino, el protocolo y el puerto. Uno de los usos más elementales de tcpdump consiste en capturar todo el tráfico de red a través de una interfaz específica. Por ejemplo, para capturar todo el tráfico de la interfaz eth0, puede utilizar la siguiente orden : Este comando capturará todos los paquetes de la interfaz eth0 y los mostrará en tiempo real en la CLI. La salida muestra las direcciones IP de origen y destino, el protocolo y otra información sobre cada paquete. Tcpdump también puede usarse para filtrar paquetes en función de varios criterios. Por ejemplo, para capturar todos los paquetes enviados desde o hacia una dirección IP específica, puede utilizar el siguiente comando : Este comando capturará todos los paquetes enviados desde o hacia la dirección IP 192.168.1.1 y los mostrará en tiempo real en la CLI. También puedes filtrar los paquetes en función del protocolo (por ejemplo, TCP, UDP), el número de puerto u otros criterios. Además de capturar paquetes en tiempo real, tcpdump también puede utilizarse para capturar paquetes en un archivo para su análisis posterior. Por ejemplo, para capturar todos los paquetes de la interfaz eth0 y registrarlos en un archivo llamado "capture.pcap", puede utilizar el siguiente comando : Este comando captura todos los paquetes de la interfaz eth0 y los registra en el archivo "capture.pcap" en formato pcap, que puede ser analizado con herramientas como Wireshark. Este comando es útil para analizar el comportamiento de la red e identificar amenazas de seguridad potenciales. ### Statut du processus El comando **ps** muestra información sobre los procesos en ejecución en un sistema Linux en la interfaz de línea de comandos, lo que es útil para identificar los procesos sospechosos de ser ejecutados en un sistema y potencialmente su seguridad. El comando puede proporcionar una gran cantidad de información sobre los procesos en ejecución, en particular su ID de proceso (PID), la utilización del usuario, del procesador y de la memoria, así como el nombre del comando. Uno de los usos más elementales del ps consiste en mostrar una lista de todos los procesos que se están ejecutando en un sistema. Por ejemplo, para mostrar una lista de todos los procesos en ejecución en el sistema, puede utilizar el comando siguiente : Este comando muestra una lista de todos los procesos en ejecución en el sistema, así como su PID, su usuario, su uso del procesador y de la memoria, así como el nombre del comando. Este comando es útil para obtener una visión de conjunto de los procesos en curso de ejecución en un sistema e identificar todo proceso sospechoso susceptible de ser ejecutado. Ps también puede utilizarse para mostrar información sobre un proceso específico o un conjunto de procesos. Por ejemplo, para mostrar información sobre un proceso con un PID específico (por ejemplo, PID 1234), puede utilizar el siguiente comando : Este comando muestra información sobre el proceso con el PID 1234, incluyendo su usuario, el uso del procesador y de la memoria, y el nombre del comando. También puede añadir información sobre un conjunto de procesos especificando varios PID. Además de mostrar información sobre los procesos en curso, ps también puede utilizarse para supervisar el estado de los procesos en tiempo real. Por ejemplo, para supervisar la utilización del procesador y de la memoria de un proceso específico (por ejemplo, el PID 1234) en tiempo real, puede utilizar el siguiente comando : Este comando muestra la utilización del procesador y de la memoria del proceso con el PID 1234 en tiempo real, manteniendo la salida todos los segundos. Este comando es útil para supervisar el rendimiento de los procesos críticos e identificar los puntos débiles del rendimiento. ### Statistiques du réseau El comando **netstat** muestra información sobre las conexiones de red activas en un sistema Linux en la CLI, lo que la hace útil para identificar las conexiones de red no autorizadas y las amenazas de seguridad potenciales. El comando puede mostrar una gran cantidad de información sobre las conexiones de red activas, incluyendo las direcciones locales y distantes, el protocolo utilizado (por ejemplo, TCP, UDP) y el estado de la conexión. Uno de los usos más elementales de netstat consiste en mostrar una lista de todas las conexiones de red activas en un sistema. Por ejemplo, para mostrar una lista de todas las conexiones de red activas, puede utilizar el siguiente comando : Este comando muestra una lista de todas las conexiones de red activas en el sistema, así como sus direcciones locales y distantes, el protocolo utilizado y el estado de la conexión. Este comando es útil para obtener una visión de conjunto de las conexiones de red activas en un sistema e identificar cualquier conexión no autorizada. Netstat también puede utilizarse para obtener información sobre las conexiones de red de un protocolo específico (por ejemplo, TCP). Por ejemplo, para mostrar una lista de todas las conexiones TCP activas en el sistema, puede utilizar el siguiente comando : Este comando muestra una lista de todas las conexiones TCP activas en el sistema, así como sus direcciones locales y distantes, y el estado de la conexión. Además de mostrar información sobre las conexiones de red activas, netstat también puede utilizarse para mostrar estadísticas de red de un protocolo específico (por ejemplo, TCP). Por ejemplo, para obtener estadísticas de todas las conexiones TCP del sistema, puede utilizar el siguiente comando : Este comando muestra estadísticas de todas las conexiones TCP del sistema, incluyendo el número de conexiones activas, el número de conexiones en cada estado y el número de errores que se muestran. Este comando es útil para supervisar el estado global de la red e identificar posibles problemas de rendimiento. ### Rechercher des fichiers El comando **find** se utiliza para buscar archivos y reportes en un sistema Linux en la CLI, lo que lo hace útil para localizar archivos y reportes específicos que pueden estar en caché o ser difíciles de encontrar. El comando busca archivos y informes en función de una gran variedad de criterios, como su nombre, tamaño, hora de modificación y autorizaciones. Uno de los usos más elementales de la búsqueda consiste en buscar ficheros y archivos por nombre. Por ejemplo, para buscar todos los archivos del directorio actual y de sus sub-repertorios con el nombre "ejemplo.txt", puede utilizar el comando siguiente : Este comando busca todos los archivos del directorio actual y de sus sub-repertorios que tengan el nombre "ejemplo.txt", y muestra su ruta completa. Find también puede utilizarse para buscar archivos y carpetas en función de su tamaño. Por ejemplo, para buscar todos los archivos del repertorio actual y de sus sub-repertorios superiores a 1 Mo, puede utilizar el comando siguiente : Este comando busca todos los archivos del directorio actual y de sus sub-registros superiores a 1 Mo , y muestra su ruta de acceso completa. Además de buscar archivos y carpetas por nombre y tamaño, la búsqueda también puede utilizarse para buscar archivos y carpetas en función de su date de modificación y de sus autorizaciones. Por ejemplo, para buscar todos los archivos del repertorio actual y de sus sub-repertorios que hayan sido modificados en el transcurso de los últimos 7 días, puede utilizar el comando siguiente : Este comando busca todos los archivos del directorio actual y de sus sub-directorios que han sido modificados en los últimos 7 días y muestra su ruta completa. En conjunto, el comando find es una herramienta poderosa para buscar archivos y reportes en un sistema Linux basado en una gran variedad de criterios, lo que lo hace útil para una variedad de tareas, incluyendo el sistema de administración y la ciberseguridad. ______ El uso de la línea de comandos Linux para la ciberseguridad puede resultar complicado para los principiantes. Sin embargo, con los comandos de base y avanzados descritos en esta guía, podrás empezar a utilizar la CLI Linux en tu beneficio en materia de ciberseguridad. No te olvides de ser prudente cuando ejecutes comandos y de comprender bien qué hace cada comando antes de utilizarlo. Para saber más sobre el uso de Linux para la ciberseguridad, descarga el sistema de explotación **[Kali Linux](https://www.kali.org/downloads/)**, que está especialmente diseñado para las pruebas de intrusión y la criminalidad numérica. ## Conclusión En conclusión, la interfaz de línea de comandos Linux es una herramienta poderosa para los profesionales de la ciberseguridad, pero puede resultar intimidante para los principiantes. Si aprendes los comandos básicos y avanzados descritos en esta guía, podrás empezar a utilizar la CLI de Linux en beneficio de la ciberseguridad. No te olvides de ser prudente a la hora de ejecutar comandos y de comprender qué hace cada comando antes de utilizarlo. Con la práctica y la experiencia, podrás dominar el uso de la línea de comandos de Linux y mejorar tu nivel de ciberseguridad.